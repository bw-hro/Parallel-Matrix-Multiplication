typedef struct{
    char name[10];
    int rows;
    int cols; 
    int **matrix;  
} matrix;

typedef struct{
    char type[10];
    double time;    
} multiReturn;

int col, row, add, m, n, p, print; //these vars are in all functions availble
int numOfProc;
char method[10];
time_t timestamp; // -same
